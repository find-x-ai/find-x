const page = async () => {
  // const session = await get_session();
  // if (!session.success) {
  //   redirect("/login");
  // }
  // console.log(session);
  return (
    <div>
      <h1>Dashboard Coming soon...</h1>
    </div>
  );
};

export default page;
