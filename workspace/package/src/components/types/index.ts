/* Config types for chat component */
export type Config = {
  findx_key: string;
  theme: string;
};

export type XMLresponse = {
  heading: string;
  content: string;
  code: string;
  steps: string;
  urls: string[]
};
